#include <stdio.h>
int main()
{
    int a,b,temp;
    printf("enter a val: ");
    scanf("%d",&a);
    printf("enter b val: ");
    scanf("%d",&b);
    temp=a;
    a=b;
    b=temp;
    printf("after swapping:a=%d,b=%d\n",a,b);
    return 0;
}
